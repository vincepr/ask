### Variables — override in .vscode/settings.json or modify inline
#@baseUrl = http://localhost:13000
@baseUrl = http://localhost:3001
@resourceDir = .

### Health check
GET {{baseUrl}}/health

### Ingest a directory (enqueues an ingest_folder job)
POST {{baseUrl}}/ingest/{{resourceDir}}/some-folder
